# 이 파일을 복사해 같은 폴더에 .env 로 저장한 뒤 값을 채우세요. (.env 는 git에 올라가지 않습니다)
# 테스트 전용 새 지갑의 비밀키만 사용하세요. 실제 자산이 든 지갑의 키는 절대 넣지 마세요.
PRIVATE_KEY=
# 비워두면 공개 노드(publicnode)를 사용합니다.
SEPOLIA_RPC_URL=
# https://etherscan.io/myapikey 에서 무료 발급 (컨트랙트 소스 공개 검증용)
ETHERSCAN_API_KEY=
